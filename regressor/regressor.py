class Regressor:     
        
    """It will return a tuple with (gain on train_set, gain on validation_set)"""
    def learn(self):
        raise("Not implemented")

    
    def finalize(self):
        raise("Not implemented")

